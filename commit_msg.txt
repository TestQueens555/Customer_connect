fix: replace waitFor locator strategy with fixed 3s settle time in CI

Previous approach: waitFor([role=group], 25s) and waitFor(RESOLUTION DESK, 25s)
caused CI job to exceed 30min timeout (26 TCs x 45s each = 19+ mins).

New approach:
- navigateToActionQueue/Details: goto + waitForTimeout(3000) for KendoUI settle
- loginAndGetPage: check session reuse to avoid re-login on every TC
- Total estimated CI time: 26 TCs x ~8s = ~3.5 mins well within 30min limit