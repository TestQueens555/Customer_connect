fix: suppress XSS alert in CI - add addInitScript to dialog handler

- Root cause: TC-AQ-025 XSS payload fires real JS alert in headless CI, blocking Playwright and producing empty test-results.json
- Fix: loginAndGetPage helper now calls page.addInitScript to override window.alert/confirm/prompt before any page JS runs
- Also adds page.on dialog handler as backup for any remaining dialogs
- This allows all 26 TCs to complete in CI without hanging on the XSS dialog
- BUG-AQ-001 (XSS) remains open - alert is suppressed for CI stability only